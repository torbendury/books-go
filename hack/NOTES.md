# Starting a local DB

```bash
# start it
docker compose up

# hit Ctrl+C to stop it or run "docker compose down" if you ran it with `-d` parameter

# stop everything
docker compose down

# prune (all!!!) volumes
docker volume prune -f
```
